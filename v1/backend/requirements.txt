# --- Web Framework ---
fastapi>=0.110.0          # REST API framework (like Express.js for Python)
uvicorn[standard]>=0.29.0 # ASGI server that runs FastAPI
python-multipart>=0.0.9   # Handles file uploads (multipart/form-data)
sse-starlette>=2.0.0      # Server-Sent Events for real-time streaming

# --- Data Manipulation ---
pandas>=2.2.0             # DataFrames — the core data structure for tabular data
numpy>=1.26.0             # Numerical computing (arrays, math)
openpyxl>=3.1.0           # Read Excel (.xlsx) files

# --- Machine Learning ---
scikit-learn>=1.4.0       # ML toolkit: preprocessing, models, evaluation
xgboost>=2.0.0            # Gradient boosting (often wins Kaggle competitions)
lightgbm>=4.3.0           # Microsoft's gradient boosting (fast + accurate)
shap>=0.45.0              # Explainability — tells you WHY a model made a prediction
joblib>=1.3.0             # Save/load ML models to disk

# --- Visualization ---
matplotlib>=3.8.0         # Chart generation (for HTML reports)
seaborn>=0.13.0           # Statistical visualizations built on matplotlib

# --- AI Agent (FREE — runs locally via Ollama) ---
langchain>=0.3.0          # Agent orchestration framework
langchain-ollama>=0.3.0   # Ollama integration (free local LLMs)
langchain-core>=0.3.0     # Core abstractions (tools, messages, etc.)

# --- Report Generation ---
jinja2>=3.1.0             # HTML template engine (like EJS/Handlebars for Python)

# --- Utilities ---
pydantic>=2.6.0           # Data validation (FastAPI uses this for request/response models)
